export default function calculateSpinalLoad(COMs, screenToRefRatio)
{
    //COMs: upperArm, foreArm, head, trunk, hand, l5s1
        //.x, .y, .mass, .load (for l5s1)
    //ToDo: Given coordinates and mass of segments in COMs

    let spinalLoad = 5;
    return spinalLoad;
}